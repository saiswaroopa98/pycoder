FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Copy source code
COPY tsconfig.json ./
COPY src ./src

# Build TypeScript and clean up
RUN npm run build && \
    npm prune --production

# Run the service
CMD ["node", "dist/index.js"]